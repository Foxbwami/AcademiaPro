services:
  - type: web
    name: academia-pro
    runtime: python-3.10
    startCommand: bash start.sh
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
